  - name: run_command
    hosts: all
    gather_facts: no 
    connection: local

    tasks:
      - name: Pull show command
        bigip_command:
          commands:
            - "dirs"
        ignore_errors: true