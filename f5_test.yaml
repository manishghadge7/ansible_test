  - name: run_command
    hosts: all
    gather_facts: no 
    connection: local

    tasks:
      - name: Pull show command
        bigip_command:
          commands:
            - "dirs"
        register: result1
        ignore_errors: true
      - debug: msg="{{result1.stdout}}"